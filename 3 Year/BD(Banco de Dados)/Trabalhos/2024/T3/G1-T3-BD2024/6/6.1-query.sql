SELECT P.NOME AS NOME_COMPRADOR, I.INSCRICAO AS INSCRICAO_IMOVEL, I.PRECO_VENDA AS PRECO_IMOVEL, C.NOME AS NOME_CORRETOR FROM PESSOA P JOIN PROMESSACOMPRA PC ON P.CPF = PC.CPF_COMPRADOR JOIN IMOVEL I ON I.INSCRICAO = PC.INSCRICAO JOIN PESSOA C ON C.CPF = PC.CPF_CORRETOR ORDER BY P.NOME;
