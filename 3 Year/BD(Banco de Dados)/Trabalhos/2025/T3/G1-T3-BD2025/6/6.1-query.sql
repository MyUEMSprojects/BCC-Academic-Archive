SELECT v.NUMERO_VOO, v.EMPRESA, a.PREFIXO, ta.NOME_TIPO FROM VOO v JOIN SEGMENTO_VOO sv ON v.NUMERO_VOO = sv.NUMERO_VOO JOIN AVIAO a ON sv.PREFIXO = a.PREFIXO JOIN TIPO_AVIAO ta ON a.NOME_TIPO = ta.NOME_TIPO WHERE sv.NUMERO_SEGMENTO = 1;
